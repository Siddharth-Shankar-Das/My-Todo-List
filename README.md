# My-Todo-List
A small, polished To-Do List web app built with **HTML**, **CSS**, and **vanilla JavaScript**.   It stores tasks in the browser's `localStorage`, supports due dates, sorting, marking tasks complete, and shows counts for pending &amp; completed tasks.
